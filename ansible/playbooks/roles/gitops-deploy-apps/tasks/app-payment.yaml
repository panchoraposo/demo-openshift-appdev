---
- name: "Ensure the '{{ payment_app_namespace }}' namespace exists"
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Namespace
      metadata:
        name: "{{ payment_app_namespace }}"
        labels:
          argocd.argoproj.io/managed-by: ansible

- name: "Create ArgoCD application for Payment Application"
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: argoproj.io/v1alpha1
      kind: Application
      metadata:
        name: app-payment
        namespace: openshift-gitops
      spec:
        project: default
        source: { repoURL: "{{ gitops_repo_apps }}", targetRevision: main, path: apps/payment/overlays/dev }
        destination: { server: https://kubernetes.default.svc }
        syncPolicy: { automated: { prune: true, selfHeal: true } }

